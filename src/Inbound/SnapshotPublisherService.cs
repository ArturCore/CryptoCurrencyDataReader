namespace Invound
{
    internal class SnapshotPublisherService
    {
        /// at the start of the project:
        /// get snapshot
        /// apply snapshot
        /// start stream
        /// publish every update
    }
}
